import { Heading } from '@adobe/react-spectrum';
import React from 'react';

export type FooterProps = {};

export const Footer: React.FC<FooterProps> = (props: FooterProps) => {
  return <Heading level={1}>test</Heading>;
};
